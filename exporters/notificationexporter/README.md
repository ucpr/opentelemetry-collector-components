<!-- status autogenerated section -->
# Notification Exporter

| Status        |           |
| ------------- |-----------|
| Stability     | [development]: logs   |
| Distributions | []        |

[development]: https://github.com/open-telemetry/opentelemetry-collector/blob/main/docs/component-stability.md#development
<!-- end autogenerated section -->

Renders each log record through a user-defined [Go `text/template`](https://pkg.go.dev/text/template) and
delivers the result as the body of an HTTP request. Supports **logs only**.

Four destination `type`s are available:

| `type`             | Destination                                                | Auth                          |
| ---                | ---                                                         | ---                            |
| `webhook` (default) | Any arbitrary webhook-shaped HTTP endpoint (Slack Incoming Webhook, PagerDuty, ...) | Whatever `headers`/`auth` you configure |
| `slack_app`         | Slack Web API (`chat.postMessage`), as a Slack App/bot      | Bot User OAuth Token (`slack_app.token`) |
| `discord_webhook`   | Discord Incoming Webhook URL                                | None (the URL is the secret)   |
| `discord_bot`       | Discord Bot API (`/channels/{id}/messages`)                 | Bot token (`discord_bot.token`) |

`slack_app`, `discord_webhook`, and `discord_bot` pre-fill the endpoint, auth headers, and a sensible
default `body_template` for that destination — `body_template`/`body_template_file` can still be set to
fully customize the payload (e.g. Slack Block Kit, Discord embeds).

This exporter intentionally does *not* implement its own routing/filtering DSL beyond a single optional
OTTL condition. The expected pipeline shape is:

```
receivers -> filter processor -> transform processor -> routing connector -> notification exporter (one per destination)
```

i.e. use `filter`/`transform` processors and a `routing` connector upstream to decide *what* reaches this
exporter and to shape the data; use this exporter only to render the final payload and deliver it.

## Configuration

| Field                 | Type   | Default | Description |
| ---                   | ---    | ---     | --- |
| `type`                 | string | `webhook` | One of `webhook`, `slack_app`, `discord_webhook`, `discord_bot`. See the table above. |
| `endpoint`             | string | (none, required for `webhook`/`discord_webhook`) | Destination URL. Inherited from [`confighttp.ClientConfig`](https://pkg.go.dev/go.opentelemetry.io/collector/config/confighttp#ClientConfig); `headers`, `tls`, `auth`, `compression`, etc. are all available the same way. Auto-filled for `slack_app` (`https://slack.com/api/chat.postMessage`) and `discord_bot` (derived from `discord_bot.channel_id`) unless overridden. |
| `method`               | string | `POST`  | HTTP method used to deliver the notification. One of `POST`, `PUT`, `PATCH`. |
| `slack_app.token`      | string | (none, required for `slack_app`) | Slack Bot User OAuth Token (`xoxb-...`). Sent as `Authorization: Bearer <token>`. |
| `slack_app.channel`    | string | (none, required for `slack_app`) | Slack channel ID or name (e.g. `C0123456` or `#alerts`) to post to. |
| `discord_bot.token`    | string | (none, required for `discord_bot`) | Discord bot token. Sent as `Authorization: Bot <token>`. |
| `discord_bot.channel_id` | string | (none, required for `discord_bot`) | Discord channel ID to post to. |
| `body_template`        | string | (none)  | Inline Go `text/template` source for the request body. Exactly one of `body_template` / `body_template_file` must be set, except `slack_app`/`discord_webhook`/`discord_bot`, which fall back to a built-in default (see below) when neither is set. |
| `body_template_file`   | string | (none)  | Path to a file containing the template source, as an alternative to `body_template`. |
| `condition`            | string | (none)  | Optional OTTL boolean condition (same syntax as `filterprocessor`'s `log_record` conditions). Records that don't match are skipped by this exporter. When unset, every record matches. Intended as defense-in-depth for standalone use — routing normally belongs upstream in a `routing` connector. |
| `sending_queue`        |        |         | See [exporterhelper queue/retry docs](https://github.com/open-telemetry/opentelemetry-collector/blob/main/exporter/exporterhelper/README.md). |
| `retry_on_failure`     |        |         | See same. |

### Template data

The body template is executed once per matched log record, against:

| Field                 | Type                | Description |
| ---                   | ---                 | --- |
| `.Body`                | `string`            | String form of the log record body. |
| `.SeverityText`        | `string`            | |
| `.SeverityNumber`      | `int32`             | |
| `.Timestamp`           | `time.Time`         | |
| `.Attributes`          | `map[string]any`    | Log record attributes. |
| `.ResourceAttributes`  | `map[string]any`    | Resource attributes. |
| `.ScopeAttributes`     | `map[string]any`    | Instrumentation scope attributes. |

### Template functions

| Function       | Signature                                  | Description |
| ---            | ---                                         | --- |
| `toJson`        | `func(v any) (string, error)`              | Marshals `v` with `encoding/json`. **Use this for every value interpolated into a JSON body** — see warning below. |
| `toJsonIndent`  | `func(v any, prefix, indent string) (string, error)` | Same, pretty-printed. |
| `join`          | `func(elems []string, sep string) string`  | `strings.Join`. |
| `lower`         | `func(s string) string`                    | `strings.ToLower`. |
| `upper`         | `func(s string) string`                    | `strings.ToUpper`. |

> **Do NOT interpolate raw strings into a JSON template. Always pipe through `toJson`.**
>
> `.Body`, `.Attributes` values, and friends come from free-text log messages — arbitrary text from
> Kubernetes controllers, applications, etc. — that can contain quotes, newlines, and backslashes. Writing
>
> ```
> {"text": "{{ .Body }}"}
> ```
>
> produces invalid JSON the moment that text contains a `"` or a newline. Write this instead:
>
> ```
> {"text": {{ .Body | toJson }}}
> ```
>
> `toJson` on a string value produces a properly quoted-and-escaped JSON string literal (including the
> surrounding quotes), so no extra `"..."` should be added around it in the template. On a map/slice value it
> produces a JSON literal that can be embedded directly in the body.

## Example: Slack incoming webhook

```yaml
exporters:
  notification/slack-warning:
    endpoint: https://hooks.slack.com/services/T000/B000/XXXXXXXXXXXXXXXXXXXXXXXX
    condition: severity_text == "Warning"
    body_template: |
      {"text": {{ printf "[%s] %s: %s" (index .ResourceAttributes "k8s.namespace.name") (index .Attributes "k8s.event.reason") .Body | toJson }}}
```

## Example: Slack App (bot token)

Posts via the Slack Web API instead of a fixed Incoming Webhook URL — useful when a Slack App/bot is
already provisioned (e.g. to post to multiple channels with one token, or to reuse an existing app's
scopes). `endpoint` and a default `body_template` are filled in automatically.

```yaml
exporters:
  notification/slack-app-warning:
    type: slack_app
    condition: severity_text == "Warning"
    slack_app:
      token: ${env:SLACK_BOT_TOKEN}
      channel: "#alerts"
```

To customize the message (e.g. Slack [Block Kit](https://api.slack.com/block-kit)), set `body_template`
explicitly — it must still include `"channel"` since that's normally supplied by the default template:

```yaml
exporters:
  notification/slack-app-warning:
    type: slack_app
    slack_app:
      token: ${env:SLACK_BOT_TOKEN}
      channel: "#alerts"
    body_template: |
      {"channel": "#alerts", "blocks": [{"type": "section", "text": {"type": "mrkdwn", "text": {{ .Body | toJson }}}}]}
```

## Example: Discord incoming webhook

```yaml
exporters:
  notification/discord-warning:
    type: discord_webhook
    endpoint: https://discord.com/api/webhooks/000000000000000000/XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
    condition: severity_text == "Warning"
```

## Example: Discord bot

Posts via the Discord Bot API instead of an Incoming Webhook URL. `endpoint` is derived from
`discord_bot.channel_id` and a default `body_template` is filled in automatically.

```yaml
exporters:
  notification/discord-bot-warning:
    type: discord_bot
    condition: severity_text == "Warning"
    discord_bot:
      token: ${env:DISCORD_BOT_TOKEN}
      channel_id: "123456789012345678"
```

## Example: generic webhook with a template file

```yaml
exporters:
  notification/generic:
    endpoint: https://example.com/hooks/otel
    headers:
      authorization: Bearer ${env:WEBHOOK_TOKEN}
    body_template_file: /etc/otelcol/templates/generic.tmpl
```

## Partial failures

Delivery is attempted independently per log record. If some records in a batch fail (non-2xx response,
network error, etc.) while others succeed, only the failed records are reported back for retry/queueing —
successfully delivered records are not resent.
